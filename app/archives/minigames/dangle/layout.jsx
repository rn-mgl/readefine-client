import React from "react";

export const metadata = {
  title: "Readefine | Dangle",
  definition: "Readefine's dangle.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}

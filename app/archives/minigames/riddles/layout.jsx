import React from "react";

export const metadata = {
  title: "Readefine | Riddles",
  definition: "Readefine's riddles.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}

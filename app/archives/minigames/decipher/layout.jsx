import React from "react";

export const metadata = {
  title: "Readefine | Decipher",
  definition: "Readefine's decipher.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}

import React from "react";

export const metadata = {
  title: "Readefine | Achievements",
  definition: "Readefine's achievements.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}

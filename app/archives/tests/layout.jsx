import React from "react";

export const metadata = {
  title: "Readefine | Tests",
  definition: "Readefine's tests.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}

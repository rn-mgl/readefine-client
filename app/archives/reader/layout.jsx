import React from "react";

export const metadata = {
  title: "Reader | Profile",
  definition: "Readefine's reader profile.",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
